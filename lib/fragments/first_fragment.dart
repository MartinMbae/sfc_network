import 'package:flutter/material.dart';
import 'package:flutter_network/pages/login.dart';

class FirstFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Welcome to SFCFIBER"
      ),
    );
  }
}