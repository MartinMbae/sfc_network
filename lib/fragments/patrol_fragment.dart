import 'package:flutter/material.dart';

class PatrolPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Patrol Content here"),
    );
  }
}
